import 'package:flutter/material.dart';

class titleSection extends StatelessWidget {
  const titleSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello Derone'),
    );
  }
}
